server:
  port: 80

spring:
  datasource:
    url:
    username:
    password:
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 3000
      maximum-pool-size: 5
      max-lifetime: 30000
      driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


cloud:
  aws:
    s3:
      bucket:
    region:
      static:
    cloud-front:
      domain-name:
      distribution-id:

